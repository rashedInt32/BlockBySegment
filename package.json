{
  "name": "blockbysegment",
  "version": "2.0.0",
  "description": "Block websites by daily time segments, with a per-segment time budget and parent PIN.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
